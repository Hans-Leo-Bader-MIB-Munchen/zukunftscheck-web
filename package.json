{
  "name": "zukunftscheck-web",
  "version": "0.1.0",
  "private": true,
  "description": "Static shell for the ZukunftsCheck web architecture.",
  "scripts": {
    "test": "node tests/preview-check.mjs",
    "build": "echo \"Static shell: no build step required.\""
  },
  "dependencies": {
    "nodemailer": "^9.0.3"
  }
}
